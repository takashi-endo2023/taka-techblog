---
title: "リバースプロキシの役割と設計——Nginx・CDN・キャッシュ戦略"
emoji: "🔀"
type: "tech"
topics: ["Nginx", "インフラ", "AWS", "DevOps"]
published: false
canonical_url: "https://www.taka-techblog.com/blog/reverse-proxy-cache-design"
---

:::message
この記事は [taka-techblog](https://www.taka-techblog.com/blog/reverse-proxy-cache-design?utm_source=zenn&utm_medium=referral) にも掲載しています。
:::

アプリサーバに直接トラフィックを当てると何が起きるか。遅いクライアントへのレスポンス送信でアプリプロセスが占有され、Keep-Aliveの管理コストが高くなり、静的ファイルもアプリが返すことになる。

リバースプロキシはこれらの問題を解決するためにアプリサーバの前段に置く中継サーバだ。

## リバースプロキシの役割

### 1. バッファリング

アプリサーバは高速なリバースプロキシにレスポンスを渡したらすぐ次のリクエストを処理できる。遅いクライアントへの送信はリバースプロキシが代わりに行う。

```
【バッファリングあり（Nginxあり）】
アプリ → Nginx（高速転送）→ クライアント（遅い回線）
アプリはNginxへの送信が終わったらすぐ解放される
```

### 2. Keep-Alive の管理

Nginx がクライアントとの Keep-Alive を管理し、アプリとは短命なコネクションで通信することでアプリの負荷を下げられる。

### 3. URLルーティング

```nginx
location /api/ {
    proxy_pass http://app-server:3000;
}

location /static/ {
    root /var/www;  # 静的ファイルはNginxが直接返す
    expires 1y;
}
```

### 4. SSL終端

クライアントとの HTTPS 通信は Nginx で終端し、内部ネットワークは HTTP で通信する。

## Nginxの基本設定

```nginx
upstream app_servers {
    server 10.0.1.10:3000;
    server 10.0.1.11:3000;
    keepalive 32;
}

server {
    listen 80;

    location / {
        proxy_pass http://app_servers;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_buffering on;
    }
}
```

## キャッシュ戦略

```nginx
proxy_cache_path /var/cache/nginx
    keys_zone=app_cache:10m
    max_size=1g
    inactive=60m;

location /api/products {
    proxy_cache app_cache;
    proxy_cache_valid 200 10m;
    add_header X-Cache-Status $upstream_cache_status;
    proxy_pass http://app_servers;
}
```

### キャッシュすべきもの・すべきでないもの

| コンテンツ | キャッシュ | 理由 |
|---|---|---|
| 静的ファイル（CSS/JS/画像） | ◎ | 変更頻度が低い |
| GETのAPIレスポンス（カタログ等） | ○ | TTLを短く設定 |
| ユーザー固有データ | ❌ | 認証情報が漏れる |
| POST / PUT / DELETE | ❌ | 副作用があるリクエスト |

## CDN との組み合わせ（CloudFront）

```
ユーザー
    ↓ キャッシュHITならエッジで返す
[CDNエッジ]
    ↓ キャッシュMISSのときだけ
[オリジン（Nginx）] → [アプリサーバ]
```

```
CloudFront Behavior:

/static/*
  → Cache Policy: CachingOptimized（1年間）

/api/*
  → Cache Policy: CachingDisabled

/*（デフォルト）
  → TTL 0〜60秒
```

### キャッシュの無効化

```bash
# CloudFrontのキャッシュを無効化
aws cloudfront create-invalidation \
  --distribution-id E1234ABCD \
  --paths "/static/*" "/index.html"
```

## まとめ

1. **アプリサーバはリバースプロキシの後ろに置く**（バッファリング・Keep-Alive管理）
2. **静的ファイルはNginxかS3/CDNが返す**（アプリを通さない）
3. **GETのキャッシュはTTLを短めに設定**（鮮度とヒット率のバランス）
4. **ユーザー固有データはキャッシュしない**（情報漏洩リスク）

リバースプロキシの設計思想は[サーバ/インフラを支える技術](https://www.amazon.co.jp/dp/4774135666)の2章で詳しく解説されている。

https://www.amazon.co.jp/dp/4774135666

---

他の記事も読む → [taka-techblog.com](https://www.taka-techblog.com?utm_source=zenn&utm_medium=referral)
X でも発信中 → [@_taka_tech](https://x.com/_taka_tech)
