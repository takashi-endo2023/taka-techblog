---
title: "MySQLレプリケーション設計——障害復旧・読み取り分散・RDS Multi-AZとの比較"
emoji: "🗄️"
type: "tech"
topics: ["MySQL", "インフラ", "AWS"]
published: false
canonical_url: "https://www.taka-techblog.com/blog/mysql-replication-design"
---

:::message
この記事は [taka-techblog](https://www.taka-techblog.com/blog/mysql-replication-design?utm_source=zenn&utm_medium=referral) にも掲載しています。
:::

DBサーバが止まるとサービス全体が止まる。アプリサーバは冗長化できても、DBがシングルポイントになっているケースは多い。

MySQLレプリケーションはDBの可用性を高めるための基本技術で、AWSのRDS Multi-AZやAurora Read Replicaもこの仕組みの上に成り立っている。

## レプリケーションの仕組み

MySQLのレプリケーションはバイナリログを使った非同期コピーだ。

1. マスタでデータ変更が発生すると**バイナリログ**に記録される
2. スレーブの**I/Oスレッド**がバイナリログを受け取り**リレーログ**に書き込む
3. スレーブの**SQLスレッド**がリレーログを再生してデータを更新する

**非同期のため、スレーブはマスタより少し遅れる（レプリケーション遅延）**。

## シングルマスタ・マルチスレーブ構成

**マスタ**: 書き込み専用（INSERT・UPDATE・DELETE）
**スレーブ**: 読み取り専用（SELECT）。複数台で読み取り負荷を分散する。

## 設定の基本（MySQL 8.0）

```sql
-- マスタ側の設定（my.cnf）
[mysqld]
server-id = 1
log-bin = mysql-bin
binlog-format = ROW  -- 推奨: ROW形式（行ベースのレプリケーション）

-- レプリケーション用ユーザを作成
CREATE USER 'repl'@'%' IDENTIFIED WITH mysql_native_password BY 'password';
GRANT REPLICATION SLAVE ON *.* TO 'repl'@'%';
```

```sql
-- スレーブ側
[mysqld]
server-id = 2
read-only = 1

CHANGE REPLICATION SOURCE TO
  SOURCE_HOST='master-host',
  SOURCE_USER='repl',
  SOURCE_PASSWORD='password',
  SOURCE_AUTO_POSITION=1;  -- GTIDを使った自動ポジション管理

START REPLICA;
```

```sql
-- 状態確認
SHOW REPLICA STATUS\G
-- Replica_IO_Running: Yes
-- Replica_SQL_Running: Yes
-- Seconds_Behind_Source: 0  ← 遅延秒数（0が理想）
```

## レプリケーション遅延への対策

書き込み直後にスレーブから読み取ると古いデータが返ることがある。

```ts
// パターン1: 書き込み後の読み取りはマスタから
async function updateAndRead(userId: number, data: UpdateInput) {
  await masterDb.update(userId, data);
  return masterDb.findOne(userId);  // スレーブではなくマスタから読む
}
```

## AWSでのマネージド対応

| 構成 | 遅延 | フェイルオーバー | 読み取り分散 | コスト |
|---|---|---|---|---|
| MySQL レプリケーション（手動） | 非同期 | 手動 | ◎ | 安い（要運用コスト） |
| RDS Multi-AZ | 同期 | 自動（〜120秒） | ❌ | 中 |
| Aurora + Read Replica | ほぼゼロ | 自動（〜30秒） | ◎ | 高い |

### RDS Multi-AZ
- **同期レプリケーション**: データ損失ゼロ
- **自動フェイルオーバー**: 障害検知から60〜120秒
- スタンバイはフェイルオーバー専用（通常は読み取り不可）

### Aurora Read Replica
- **共有ストレージ**: レプリカラグがほぼゼロ
- **最大15台のRead Replicaを追加可能**
- **フェイルオーバー時間**: 約30秒（RDS Multi-AZより高速）

レプリケーションの設計思想は[サーバ/インフラを支える技術](https://www.amazon.co.jp/dp/4774135666)の2章で詳しく解説されている。

https://www.amazon.co.jp/dp/4774135666

---

他の記事も読む → [taka-techblog.com](https://www.taka-techblog.com?utm_source=zenn&utm_medium=referral)
X でも発信中 → [@_taka_tech](https://x.com/_taka_tech)
